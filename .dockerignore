# Coisas que NAO precisam ir pro contexto de build (mais rapido + mais seguro)
node_modules
dist
.git
.gitignore
.env
.env.local
.env.bak-*
*.log
.DS_Store
.claude/
.vscode/
.idea/
tsconfig.tsbuildinfo
tsconfig.node.tsbuildinfo

# Nota: scripts/ NAO esta ignorado pois o Dockerfile.sync precisa dele.
# Pro Dockerfile (frontend) os arquivos sao copiados mas nao vao pro stage
# final (so o /dist do builder vai). Nao tem custo real, so ~16MB extras
# no contexto de build.

# Documentos auxiliares
README.md
CLAUDE.md

# Logs e dumps
logs/
ativos.json
links_all.json
duvida_acoes.json
duvida_tecnica.json

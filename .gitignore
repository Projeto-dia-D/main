node_modules
dist
.env
.env.local
*.log
.DS_Store

# Build artifacts e config local
.claude/
tsconfig.tsbuildinfo

# Logs locais do backfill diário (gerados pelo Task Scheduler)
logs/

# Supabase CLI cache
supabase/.temp/
supabase/.branches/

# Backups do .env (CONTEM SEGREDOS) — nunca commitar
.env.bak*

# Python cache
__pycache__/
*.pyc

# Build artifact (mesma classe do tsconfig.tsbuildinfo)
tsconfig.node.tsbuildinfo

# Scripts de debug com TOKEN HARDCODED — mover o segredo pro .env antes de versionar
scripts/_fix_now.py
scripts/_refix_auth_emails.py
scripts/_query_iris_chat.py
